# local Nomad data / config overrides (contains API keys, never commit)
.nomad/
.nomad.*.json

# Go
/bin/
*.exe
*.test
*.out
coverage.txt

# editor / OS
.idea/
.vscode/
.DS_Store
CLAUDE.local.md

# vendored reference copies of charmbracelet repos (local only)
charmbracelet/

# local-only e2e tests
/e2e/

# local codex workspace
/codex/

# local agent workspace
.claude/

# embedded web-view build artifacts (run `make web`); source lives in frontend/
/internal/webview/dist/*
!/internal/webview/dist/.gitkeep
internal/webview/frontend/node_modules/
